#!/usr/bin/env bash

clear
set -x
expressvpn status
cat <<EOF


EOF
,width_reduce.sh &
cat <<EOF


EOF
sudo apt update -y && sudo apt upgrade -y
# sudo apt autoremove -y
# sudo systemctl restart NetworkManager
cat <<EOF


EOF
sudo yt-dlp -U
set +x
